export interface IComponent {
  init(
    el: HTMLElement,
    props: {
      [key: string]: any
    }
  ): void
  destroy(): void
}

export type RefValue = Set<string>

export type ReturnDOMRef<T> = {
  refs: T
}

export type DOMRef = <T>(ref: RefValue) => ReturnDOMRef<T>
