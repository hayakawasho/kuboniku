<div class="o-wrap">
  <h1 class="works-heading | relative mt-[10rem] mb-[6rem]">
    Works
    <sup class="absolute top-[0.5em] ml-[0.5em]">{{ props.total }}</sup>
  </h1>
  <div class="o-grid | js-works" data-total="{{ props.total }}">
  {% for i in props.metadata %}
    {%
      set j = {
        ttl: i.title | safe,
        slug: i.slug,
        src: i.eyecatch.pc.src,
        width: i.eyecatch.pc.w,
        height: i.eyecatch.pc.h,
        num: (props.total - loop.index0) | zeroPadding("2")
      }
    %}
    {% include './_entry.njk' %}
  {% endfor %}
  </div>
  <div class="js-works__trigger"></div>
</div>
