// import type { SvelteComponent } from 'svelte'

export class ConnectWithSvelte {
  // constructor(target: any, FC: any, props: {}) {
  // const refs = Array.from(target.querySelectorAll('[data-ref]'))
  // new FC({
  //   target,
  //   props: {
  //     ...props,
  //     refs,
  //   },
  // })
  // }
}
