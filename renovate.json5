{
  "extends": [
    "config:base",
    ":timezone(Asia/Tokyo)"
  ],
  "automerge": true,
  "schedule": ["after 10pm and before 5am every weekday", "every weekend"],
  // Limit PRs to avoid conflicts
  "prConcurrentLimit": 1,
}
